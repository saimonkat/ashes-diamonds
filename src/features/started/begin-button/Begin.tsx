import React from "react";

//style
import style from "./begin.module.scss";

const Begin = () => {
  return (
    <button className={style.begin} type={"submit"}>
      Begin the Journey
    </button>
  );
};

export default Begin;
